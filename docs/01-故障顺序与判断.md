# 01-故障顺序与判断

## 阶段 1：插件入口消失

现象：

```text
Browser 插件不可用
Chrome 插件不可用
Computer Use 插件不可用
插件市场搜不到 Browser / Chrome / Computer Use
```

判断：

```text
openai-bundled marketplace 没有被 Codex 正确发现。
```

关键路径：

```text
%USERPROFILE%\.codex\.tmp\bundled-marketplaces\openai-bundled
```

应存在：

```text
openai-bundled\.agents\plugins\marketplace.json
openai-bundled\plugins\browser\.codex-plugin\plugin.json
openai-bundled\plugins\chrome\.codex-plugin\plugin.json
openai-bundled\plugins\computer-use\.codex-plugin\plugin.json
```

技术原理：

```text
Browser / Chrome / Computer Use 是 Codex 内置 openai-bundled 插件。
marketplace 缺失时，即使 config.toml 启用了插件，线程也不会加载工具。
```

---

## 阶段 2：Chrome 扩展 Disconnected

现象：

```text
Chrome 扩展显示 Disconnected
```

判断：

```text
Chrome Native Messaging Host 没有生成或注册。
```

关键文件：

```text
%LOCALAPPDATA%\OpenAI\extension\com.openai.codexextension.json
```

关键注册表：

```text
HKCU\Software\Google\Chrome\NativeMessagingHosts\com.openai.codexextension
```

关键字段：

```json
"allowed_origins": [
  "chrome-extension://hehggadaopoacecdllhhajmbjkdcmajg/"
]
```

技术原理：

```text
Chrome 扩展必须通过 Native Messaging Host 调用本地 extension-host。
```

---

## 阶段 3：Chrome Connected 但仍不能打开网页

现象：

```text
Chrome 扩展已 Connected
但 @Chrome / @Browser 仍不能打开网页
Computer Use 仍不可用
```

判断：

```text
Chrome 扩展链路已通，但 Codex 运行时执行入口缺失。
不要继续重装 Chrome 扩展。
```

关键日志：

```text
mcp__node_repl__js
node-repl-missing
missingHelperPath=true
browser_use_setup_failed
```

技术原理：

```text
Connected 只代表 Chrome 扩展与 Native Host 连通。
真正控制网页还需要 node_repl、cua_node、codex.exe、rg.exe、sandbox helper、command runner。
```

---

## 阶段 4：WindowsApps 迁移失败

现象：

```text
bundled_executable_relocation_failed
普通复制失败
错误：无法加密指定的文件
错误 6000
```

判断：

```text
WindowsApps 中的 MSIX 文件带特殊保护或加密属性。
普通复制会失败。
```

正确方向：

```text
用文件流复制，只复制文件内容，不复制 WindowsApps 的特殊属性。
```

---

## 阶段 5：复制到 bin 根目录仍不够

曾经补齐：

```text
%LOCALAPPDATA%\OpenAI\Codex\bin\codex.exe
%LOCALAPPDATA%\OpenAI\Codex\bin\node.exe
%LOCALAPPDATA%\OpenAI\Codex\bin\node_repl.exe
%LOCALAPPDATA%\OpenAI\Codex\bin\rg.exe
%LOCALAPPDATA%\OpenAI\Codex\bin\cua_node
```

但仍失败。

原因：

```text
Codex 实际读取 fingerprint 版本化目录，不一定读取 bin 根目录。
```

---

## 阶段 6：早期包按版本化目录修复

本次有效路径：

```text
bin\38dff8711e296435\codex.exe
bin\ada252862d154cdd\rg.exe
runtimes\cua_node\1b23c930bdf84ed6\bin\node.exe
runtimes\cua_node\1b23c930bdf84ed6\bin\node_repl.exe
```

判断：

```text
必须从最新日志读取 CodexFingerprint / RgFingerprint / CuaFingerprint / RuntimePaths。
不要复用旧 hash。
```

---

## 阶段 7：CODEX_CLI_PATH 与 nodeRepl.config

现象：

```text
所有网站都被 Codex 策略阻止
但设置中是“始终允许”
网站权限列表为空
```

判断：

```text
不是网站黑名单，而是 nodeRepl.config 权限桥失败。
```

处理：

```text
设置 CODEX_CLI_PATH 指向 Desktop 实际使用且已验证版本一致的 codex.exe。
旧包可能位于 fingerprint 目录，新包也可能使用 bin 根目录；以最新 session、运行时配置和当前包为准。
```

---

## 阶段 8：sandbox / command-runner 缺失

现象：

```text
node_repl 能启动到下一阶段
但提示缺少 codex-windows-sandbox-setup.exe
或缺少 codex-command-runner.exe
```

判断：

```text
不是回到原点，而是同一运行时迁移失败链上的下一层 helper 缺失。
```

处理：

```text
只补缺失 helper。
不要回头重修 Chrome 扩展、Native Host、openai-bundled 或 node_repl。
```

---

## 阶段 9：最终成功

成功证据：

```text
Chrome 成功打开 openai.com
页面标题读取正常：OpenAI | Research & Deployment
```

---

## 阶段 10：旧线程仍不注入 node_repl

现象：

```text
Browser / Chrome / Computer Use 插件均已安装启用
node_repl.exe、node.exe、codex.exe 都存在
Chrome Native Host v2 manifest 存在
codex doctor 不再提示运行时缺失
但当前线程 tool_search("node_repl js") 仍只返回无关工具
mcp__node_repl__js 仍不可调用
```

判断：

```text
这不是 Chrome 扩展问题，也不是 node_repl.exe 文件缺失。
常见原因是旧线程的工具表没有重新注入，尤其是线程创建于旧 CLI / 旧插件状态。
```

检查信号：

```text
state_*.sqlite 的 threads 表中：
cli_version 低于当前 codex --version
history_mode=legacy
source=vscode 或旧来源
thread_dynamic_tools 为空
```

补救方向：

```text
先确认 Browser / Chrome / Computer Use 已安装启用。
再确认 node_repl.exe 可作为 MCP server 启动。
再比较当前 MSIX、活动 marketplace 和插件缓存的版本。
不要把 node_repl 注册成普通外部 stdio MCP；它可能遮蔽官方可信工具。
刷新 marketplace/插件后需要完全重启 Codex 并新建任务。
```

---

## 阶段 11：工具已注入，但 Computer Use 授权桥缺失

现象：

```text
mcp: node_repl/js started
能够执行 sky.list_apps
设置页已经允许“任意应用”
但控制具体应用时报：
Computer Use requires app approval but elicitations are unavailable
```

判断：

```text
node_repl 工具入口已经恢复，但受信任桌面桥不完整。
持久权限开关与逐应用审批不是同一层。
普通 stdio MCP 可恢复 js 工具，不一定提供 createElicitation/nativePipe。
```

检查顺序：

```text
1. 对应 session 是否真正调用 node_repl
2. session_meta.cli_version
3. Desktop 本地 codex.exe 与已安装 CLI 的版本和 SHA-256
4. 本地 node_repl 与当前 MSIX manifest 的版本和 SHA-256
5. NODE_REPL_NODE_MODULE_DIRS 是否指向匹配运行时
```

处理见 `07-授权已开启但-Computer-Use-仍失败.md`。

---

## 阶段 12：Chrome 已连接，但导航被浏览器安全层拦截

现象：

```text
Chrome 插件连接成功
能够读取当前标签页
但按站点名称导航时被浏览器安全层拦截
```

判断：

```text
连接链路正常，不是扩展安装失败。
需要用用户明确提供的完整 URL 区分安全策略和连接故障。
```

复测：

```text
@Chrome 打开 https://example.com
```

不得修改插件绕过浏览器安全层。

---

## 阶段 13：Codex 已更新，但 bundled 插件仍是旧版

现象：

```text
Codex/MSIX 版本已经更新
codex plugin list 仍显示数周前的 Chrome / Computer Use 版本
Native Host 路径仍指向旧版本缓存
工具曾出现但报 Browser security unavailable outside node repl
```

判断：

```text
%USERPROFILE%\.codex\.tmp\bundled-marketplaces\openai-bundled 没有随 MSIX 更新。
installed/enabled 只说明配置开关存在，不代表插件版本正确。
```

处理：

```text
完全退出 Codex、Chrome 和 Edge。
用当前 MSIX 的 app\resources\plugins\openai-bundled 刷新活动 marketplace。
删除旧的外部 node_repl MCP workaround。
重新安装当前版本 Chrome / Computer Use 插件。
最后把 Native Host manifest 指向新版本插件缓存。
```

---

## 推荐总排查顺序

```text
插件入口 → 当前 MSIX/marketplace/cache 插件版本 → Chrome Native Host
→ 最新 session 精确错误 → 工具是否实际调用 → 失败任务 CLI 版本 → Desktop 本地 CLI → 当前 MSIX CUA manifest
→ node_repl 与 node_modules 一致性 → 授权桥 → 明确 URL 的 Chrome 安全复测
```
